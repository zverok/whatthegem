RSpec.describe WhatTheGem::Gem::Bundled do
  describe '.fetch' do
    context 'when exists' do
    end

    context 'when not in Gemfile'

    context 'when not in Gemfile.lock'

    context 'when no Gemfile'
  end
end